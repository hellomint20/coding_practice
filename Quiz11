//문자열 섞기
//ex) str1 = 'aaaaa', str2 = 'bbbbb'==> ababababab

class Solution {
    public String solution(String str1, String str2) {
        String answer = "";
        
        for(int i=0;i<str1.length();i++){
            answer += str1.substring(i,i+1);
            answer += str2.substring(i,i+1);
        }return answer;
    }
}

//substring 함수를 이용하여 i번째 해당하는 인덱스로 문자열을 잘라서 결과 추출함

